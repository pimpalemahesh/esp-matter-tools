/**
 * CDN and asset paths for the MFG web UI.
 */
export const PYODIDE_INDEX_URL =
  'https://cdn.jsdelivr.net/pyodide/v0.24.1/full/';

/** Loaded via fetch(); URL is resolved relative to the page (same folder as index.html). */
export const MFG_PYTHON_RUNTIME = 'py/mfg_web_runtime.py';
