# Copyright 2026 Espressif Systems (Shanghai) PTE LTD
#
# Per-role MCORE default profile for a Controller (administers other nodes).

role: controller

seeds:
  - MCORE.ROLE.CONTROLLER

deny:
  - MCORE.ROLE.COMMISSIONER

feature_area_gates:
  ota_requestor:
    - MCORE.OTA.HTTPS
    - MCORE.OTA.RequestorConsent
    - MCORE.OTA.Resume
    - MCORE.OTA.Retry
  bridge:
    - MCORE.BRIDGE
    - MCORE.BRIDGE.*
  ota_provider:
    - MCORE.OTA.Provider
  icd:
    - MCORE.SC.SIT_ICD
  # The two RetrieveLogsResponse timestamp-field questions apply only when the
  # node hosts the Diagnostic Logs cluster -- a declared Root Node offering.
  diagnostic_logs:
    - MCORE.DLOG.*
